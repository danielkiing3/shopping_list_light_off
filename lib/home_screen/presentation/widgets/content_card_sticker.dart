import 'package:flutter/material.dart';

class ContentCardSticker extends StatelessWidget {
  const ContentCardSticker({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
